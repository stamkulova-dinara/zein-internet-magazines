import React, { useEffect } from 'react'

const Favorites = () => {
  useEffect(() => {
    localStorage.getItem(JSON.parse(fa))
  })

  return (
    <div>
      
    </div>
  )
}

export default Favorites